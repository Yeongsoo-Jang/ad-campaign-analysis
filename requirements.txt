streamlit==1.30.0
pandas==2.1.3
numpy==1.26.3
plotly==5.18.0
statsmodels==0.14.1
scikit-learn==1.3.2
xgboost==2.0.3
pyyaml==6.0.1
jinja2==3.1.2
requests==2.31.0
matplotlib==3.8.2
seaborn==0.13.0

# 보고서 생성 기능 (선택사항)
# macOS에서는 추가로 다음 명령 실행 필요: 
# brew install gobject-introspection pango gdk-pixbuf libffi
weasyprint==60.1

# 이메일 송신 (선택사항)
# 설정에 SMTP 서버 정보 필요